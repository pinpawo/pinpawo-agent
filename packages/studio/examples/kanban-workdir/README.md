# Kanban Studio workdir

This example shows the per-Pet Capability directory convention. Run it through
an application composition root that resolves the configured `kanban` Plugin.
The Studio package itself does not contain a concrete Plugin catalog.
