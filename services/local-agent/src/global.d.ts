/**
 * Global `require` created by tsup's banner:
 *   import { createRequire as __pinpawoCreateRequire } from 'module';
 *   const require = __pinpawoCreateRequire(import.meta.url);
 *
 * Declaring it here so bundled code can reference `require()` when needed.
 */
declare const require: NodeRequire;
